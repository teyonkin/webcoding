<?php
    $num = 10;
    // ++가 변수 뒤에 위치 하므로 10을 반환 후 ++가 작동하여 변수 num의 값은 11이됨
    echo "++가 변수 뒤에 붙어 10을 반환 후 증가하므로 num의 값은 ".$num++;

    //줄바꿈
    echo "<br />";

    //값이 11이 되었는지 확인
    echo "앞에서 num의 값이 출력되고 ++가 작동했으므로 num의 값은 ".$num;
?>
